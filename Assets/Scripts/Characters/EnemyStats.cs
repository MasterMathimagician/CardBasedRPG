using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/// This is a container class that holds all on an enemies data
/// contains a character class object and a card list object
/// passes those objects to the battleManager
public class EnemyStats : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
